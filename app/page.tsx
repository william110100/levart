import { Footer, Section } from '@/components';
import { NextPage } from 'next';

const Page: NextPage = () => {
  return (
    <main className="min-h-max w-screen">
      <Section />
      <Footer />
    </main>
  );
};

export default Page;
